-- grug-far.nvim — find & replace across files with a live preview buffer
-- LazyVim binds: <leader>sr to open (pre-fills with current file's extension)
return {
  "MagicDuck/grug-far.nvim",
  opts = {
    -- headerMaxWidth = 80,
    -- engine = "ripgrep",      -- "ripgrep" | "astgrep"
    -- resultsSeparatorLineChar = "─",
    -- wrap = true,
  },
}
